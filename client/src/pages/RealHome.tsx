// Industry Niche Atlas style reminder: editorial cartography, warm mineral paper, ink typography, oxidized teal paths, coral caveats, ochre evidence.

import { useEffect, useMemo, useState, type ReactNode } from "react";
import { ArrowRight, BookOpen, Check, ChevronRight, Database, ExternalLink, Filter, GitBranch, Layers3, Map, Search, Share2, Sparkles, Target } from "lucide-react";
import InfiniteAtlas from "@/components/InfiniteAtlas";
import AtlasRoadmaps, { type RoadmapContext } from "@/components/AtlasRoadmaps";
import GlobalVentureAtlas from "@/components/GlobalVentureAtlas";

type SourceRef = { id: string; publisher: string; vintage: string; url: string };
type Taxonomy = { id: string; code: string; title: string; level: number; taxonomy: string; source: SourceRef };
type Occupation = { id: string; soc: string; title: string; description: string; metrics: { taskCount: number; skillCount: number; workActivityCount: number; softwareCount: number; alternateTitleCount: number }; tasks: { id: string; text: string; type: string; date: string }[]; skills: { name: string; importance: number | null; date: string }[]; workActivities: string[]; software: string[]; alternateTitles: string[]; laborMarket?: { employment: number | null; medianAnnualWage: number | null; meanAnnualWage: number | null; locationQuotient: number | null; source: SourceRef } | null; source: SourceRef };
type Manifest = { releaseId: string; generatedAt: string; mode: string; counts: Record<string, number>; sources: (SourceRef & { title: string; license: string; sha256: string; file: string })[]; integrity: { syntheticRecords: number; unresolvedIndustryOccupationCrosswalk: boolean; workRhythmData: string } };
type Release = { manifest: Manifest; taxonomies: Taxonomy[]; occupations: Occupation[] };

const money = (value?: number | null) => value == null ? "Not available" : `$${Math.round(value).toLocaleString()}`;
const integer = (value?: number | null) => value == null ? "Not available" : Math.round(value).toLocaleString();
const levelLabel = (level: number, taxonomy: string) => taxonomy.startsWith("NAICS") ? ["", "", "sector", "subsector", "industry group", "NAICS industry", "national industry"][level] ?? `level ${level}` : ["", "section", "division", "group", "class"][level] ?? `level ${level}`;

function Brand() { return <a href="#top" className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl border border-[#9acdc1] bg-[#e0f0eb] font-display text-2xl text-[#0f766e]">✣</span><span className="leading-none"><span className="block font-display text-[1.05rem]">Industry Niche</span><span className="block font-display text-[1.05rem] italic text-[#0f766e]">Atlas</span></span></a>; }

function Badge({ children, tone = "teal" }: { children: ReactNode; tone?: "teal" | "coral" | "ochre" | "ink" }) { const tones = { teal: "border-[#9acdc1] bg-[#e0f0eb] text-[#0f625b]", coral: "border-[#edb6a7] bg-[#fae4de] text-[#9d483c]", ochre: "border-[#dfc681] bg-[#f6ebc9] text-[#8a6417]", ink: "border-[#c9c6ba] bg-[#e9e7df] text-[#3f423e]" }; return <span className={`inline-flex items-center rounded-full border px-2.5 py-1 font-mono text-[10px] uppercase tracking-[0.12em] ${tones[tone]}`}>{children}</span>; }

function SourceBadge({ source }: { source: SourceRef }) { return <a href={source.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 font-mono text-[10px] uppercase tracking-[0.12em] text-[#0f766e] hover:text-[#c75b4a]"><span>{source.publisher} · {source.vintage}</span><ExternalLink className="h-3 w-3" /></a>; }

function TaxonomyRail({ items, selected, setSelected }: { items: Taxonomy[]; selected: Taxonomy | null; setSelected: (item: Taxonomy) => void }) {
  const top = items.filter((item) => item.level <= 3).slice(0, 42);
  return <div className="space-y-1">{top.map((item) => <button key={item.id} onClick={() => setSelected(item)} className={`flex w-full items-start gap-3 border-b border-[#e2dfd4] px-3 py-3 text-left transition hover:bg-[#f0ede3] ${selected?.id === item.id ? "bg-[#e0f0eb]" : ""}`}><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${selected?.id === item.id ? "bg-[#0f766e]" : "bg-[#b7b7aa]"}`} /><span className="min-w-0"><span className="block font-mono text-[10px] uppercase tracking-[0.12em] text-[#77766d]">{item.code} · {levelLabel(item.level, item.taxonomy)}</span><span className="mt-1 block truncate text-sm font-medium text-[#353831]">{item.title}</span></span><ChevronRight className="ml-auto mt-1 h-4 w-4 shrink-0 text-[#a1a197]" /></button>)}</div>;
}

function TaxonomyExplorer({ data }: { data: Release }) {
  const [taxonomyName, setTaxonomyName] = useState("NAICS 2022");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState<Taxonomy | null>(null);
  const items = useMemo(() => data.taxonomies.filter((item) => item.taxonomy === taxonomyName), [data.taxonomies, taxonomyName]);
  const filtered = useMemo(() => items.filter((item) => `${item.code} ${item.title}`.toLowerCase().includes(query.toLowerCase())).slice(0, 90), [items, query]);
  const active = selected && selected.taxonomy === taxonomyName ? selected : items[0] ?? null;
  const children = active ? items.filter((item) => item.level === active.level + 1 && (active.code.length < 2 || item.code.startsWith(active.code))).slice(0, 80) : [];
  return <section id="industries" className="container py-16 lg:py-24"><div className="flex flex-col justify-between gap-5 border-b border-[#d8d5c8] pb-8 lg:flex-row lg:items-end"><div><div className="eyebrow text-[#0f766e]">02 / Formal industry trees</div><h2 className="mt-4 max-w-3xl font-display text-4xl leading-[0.98] tracking-[-0.03em] sm:text-5xl">Count the tree without pretending it is the whole forest.</h2><p className="mt-5 max-w-2xl text-[1.03rem] leading-7 text-[#64655c]">These are official activity classifications. They are precise about what they classify—and silent about the actual tasks performed inside a job.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => setTaxonomyName("NAICS 2022")} className={`rounded-full border px-4 py-2 text-xs font-semibold ${taxonomyName === "NAICS 2022" ? "border-[#0f766e] bg-[#0f766e] text-white" : "border-[#c9c6ba] bg-[#fbfaf5] text-[#4b4d46]"}`}>NAICS 2022</button><button onClick={() => setTaxonomyName("ISIC Rev. 5")} className={`rounded-full border px-4 py-2 text-xs font-semibold ${taxonomyName === "ISIC Rev. 5" ? "border-[#0f766e] bg-[#0f766e] text-white" : "border-[#c9c6ba] bg-[#fbfaf5] text-[#4b4d46]"}`}>ISIC Rev. 5</button></div></div><div className="mt-8 grid gap-5 lg:grid-cols-[280px_1fr]"><aside className="overflow-hidden rounded-2xl border border-[#d1cec2] bg-[#fbfaf5]"><div className="border-b border-[#dad7cc] bg-[#f0ede3] p-4"><div className="coordinate-stamp">DIRECTORY / {taxonomyName}</div><div className="relative mt-3"><Search className="absolute left-3 top-2.5 h-4 w-4 text-[#8a897f]" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search codes or labels" className="w-full rounded-xl border border-[#c9c6ba] bg-[#fbfaf5] py-2 pl-9 pr-3 text-xs outline-none focus:ring-2 focus:ring-[#0f766e]" /></div></div><div className="max-h-[520px] overflow-auto p-2">{filtered.length ? filtered.map((item) => <button key={item.id} onClick={() => setSelected(item)} className={`flex w-full items-start gap-2 rounded-xl px-3 py-2.5 text-left hover:bg-[#f0ede3] ${active?.id === item.id ? "bg-[#e0f0eb]" : ""}`}><span className="font-mono text-[10px] text-[#77766d]">{item.code}</span><span className="min-w-0 truncate text-xs font-medium text-[#41433c]">{item.title}</span></button>) : <p className="p-4 text-sm text-[#77766d]">No source record matches that search.</p>}</div></aside><main className="min-w-0 rounded-2xl border border-[#d1cec2] bg-[#fbfaf5] p-5 sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="eyebrow text-[#77766d]">Selected classification</div><h3 className="mt-2 font-display text-3xl text-[#242822]">{active?.title ?? "Select a classification"}</h3>{active && <div className="mt-3 flex flex-wrap gap-2"><Badge>{active.code}</Badge><Badge tone="ochre">{levelLabel(active.level, active.taxonomy)}</Badge></div>}</div>{active && <SourceBadge source={active.source} />}</div><div className="mt-7 border-y border-[#dad7cc] py-5"><div className="coordinate-stamp">NEXT LEVEL / OFFICIAL CHILD RECORDS</div><div className="mt-4 grid gap-2 sm:grid-cols-2">{children.length ? children.map((item) => <button key={item.id} onClick={() => setSelected(item)} className="group flex items-center justify-between border border-[#dedbd0] bg-[#f5f1e7] px-4 py-3 text-left hover:border-[#9acdc1]"><span><span className="block font-mono text-[10px] text-[#77766d]">{item.code}</span><span className="mt-1 block text-sm font-medium text-[#353831]">{item.title}</span></span><ChevronRight className="h-4 w-4 text-[#0f766e] transition-transform group-hover:translate-x-1" /></button>) : <p className="text-sm leading-6 text-[#6b6c64]">This release preserves the official record but does not invent a child relationship where the source structure does not expose one.</p>}</div></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><div className="mini-stat"><div className="eyebrow text-[#77766d]">Tree records</div><div className="mt-2 font-display text-2xl text-[#242822]">{integer(items.length)}</div></div><div className="mini-stat"><div className="eyebrow text-[#77766d]">Maximum depth</div><div className="mt-2 font-display text-2xl text-[#242822]">L{Math.max(...items.map((item) => item.level), 0)}</div></div><div className="mini-stat"><div className="eyebrow text-[#77766d]">Crosswalk status</div><div className="mt-2 text-xs leading-5 text-[#9d483c]">Not inferred</div></div></div></main></div></section>;
}

function OccupationDetail({ occupation }: { occupation: Occupation }) { const [tab, setTab] = useState<"profile" | "tasks" | "skills">("profile"); return <div className="rounded-2xl border border-[#d1cec2] bg-[#fbfaf5] p-5 sm:p-7"><div className="flex flex-col justify-between gap-5 border-b border-[#dad7cc] pb-5 sm:flex-row sm:items-start"><div><div className="eyebrow text-[#c75b4a]">O*NET-SOC / {occupation.id}</div><h3 className="mt-2 font-display text-3xl text-[#242822]">{occupation.title}</h3><p className="mt-4 max-w-2xl text-sm leading-6 text-[#686960]">{occupation.description}</p></div><SourceBadge source={occupation.source} /></div><div className="mt-5 grid grid-cols-2 gap-2 sm:grid-cols-5"><div className="mini-stat"><div className="eyebrow">Tasks</div><div className="mt-2 font-display text-xl">{integer(occupation.metrics.taskCount)}</div></div><div className="mini-stat"><div className="eyebrow">Skills</div><div className="mt-2 font-display text-xl">{integer(occupation.metrics.skillCount)}</div></div><div className="mini-stat"><div className="eyebrow">Activities</div><div className="mt-2 font-display text-xl">{integer(occupation.metrics.workActivityCount)}</div></div><div className="mini-stat"><div className="eyebrow">Median wage</div><div className="mt-2 font-display text-xl">{money(occupation.laborMarket?.medianAnnualWage)}</div></div><div className="mini-stat"><div className="eyebrow">Employment</div><div className="mt-2 font-display text-xl">{integer(occupation.laborMarket?.employment)}</div></div></div><div className="mt-7 flex gap-2 border-b border-[#dad7cc]"><button onClick={() => setTab("profile")} className={`border-b-2 px-3 py-2 text-xs font-semibold ${tab === "profile" ? "border-[#0f766e] text-[#0f766e]" : "border-transparent text-[#77766d]"}`}>Profile</button><button onClick={() => setTab("tasks")} className={`border-b-2 px-3 py-2 text-xs font-semibold ${tab === "tasks" ? "border-[#0f766e] text-[#0f766e]" : "border-transparent text-[#77766d]"}`}>Task statements</button><button onClick={() => setTab("skills")} className={`border-b-2 px-3 py-2 text-xs font-semibold ${tab === "skills" ? "border-[#0f766e] text-[#0f766e]" : "border-transparent text-[#77766d]"}`}>Skills & activity</button></div>{tab === "profile" && <div className="mt-6 grid gap-7 lg:grid-cols-2"><div><div className="coordinate-stamp">ALT TITLES / SOURCE RECORDS</div><div className="mt-3 flex flex-wrap gap-2">{occupation.alternateTitles.slice(0, 16).map((title) => <Badge key={title} tone="ink">{title}</Badge>)}</div></div><div><div className="coordinate-stamp">LABOR MARKET / BLS MAY 2025</div>{occupation.laborMarket ? <div className="mt-3 grid grid-cols-2 gap-3 text-sm"><p><span className="block text-xs text-[#77766d]">Mean annual wage</span><strong className="mt-1 block text-[#242822]">{money(occupation.laborMarket.meanAnnualWage)}</strong></p><p><span className="block text-xs text-[#77766d]">Location quotient</span><strong className="mt-1 block text-[#242822]">{occupation.laborMarket.locationQuotient ?? "Not available"}</strong></p></div> : <p className="mt-3 text-sm leading-6 text-[#9d483c]">No matching detailed BLS cross-industry record was found for this O*NET-SOC code in the current release.</p>}</div></div>}{tab === "tasks" && <div className="mt-6 space-y-3">{occupation.tasks.map((task) => <div key={task.id} className="border-l-2 border-[#0f766e] bg-[#f5f1e7] px-4 py-3"><div className="font-mono text-[10px] uppercase tracking-[0.12em] text-[#77766d]">Task {task.id} · {task.type} · {task.date}</div><p className="mt-2 text-sm leading-6 text-[#454740]">{task.text}</p></div>)}</div>}{tab === "skills" && <div className="mt-6 grid gap-7 lg:grid-cols-2"><div><div className="coordinate-stamp">ESSENTIAL SKILLS / IMPORTANCE</div><div className="mt-4 space-y-3">{occupation.skills.slice(0, 10).map((skill) => <div key={skill.name}><div className="flex justify-between text-sm"><span>{skill.name}</span><span className="font-mono text-xs text-[#77766d]">{skill.importance ?? "—"}</span></div><div className="mt-1 h-2 bg-[#e9e6dc]"><div className="h-2 bg-[#5ca99d]" style={{ width: `${Math.min((skill.importance ?? 0) * 20, 100)}%` }} /></div></div>)}</div></div><div><div className="coordinate-stamp">WORK ACTIVITIES / SOFTWARE</div><div className="mt-4 flex flex-wrap gap-2">{occupation.workActivities.slice(0, 12).map((item) => <Badge key={item}>{item}</Badge>)}{occupation.software.slice(0, 12).map((item) => <Badge key={item} tone="ochre">{item}</Badge>)}</div></div></div>}</div>; }

function OccupationExplorer({ data, selected, setSelected }: { data: Release; selected: Occupation; setSelected: (occupation: Occupation) => void }) { const [query, setQuery] = useState(""); const filtered = useMemo(() => data.occupations.filter((item) => `${item.title} ${item.id} ${item.alternateTitles.join(" ")}`.toLowerCase().includes(query.toLowerCase())).slice(0, 120), [data.occupations, query]); const scatter = useMemo(() => data.occupations.filter((item) => item.laborMarket?.medianAnnualWage && item.metrics.taskCount > 0).slice(0, 420), [data.occupations]); return <section id="work" className="border-y border-[#d8d5c8] bg-[#f5f1e7]"><div className="container py-16 lg:py-24"><div className="mb-8 flex items-center justify-between border-y border-[#d8d5c8] py-3"><span className="coordinate-stamp">WORK GRAPH / O*NET + BLS</span><span className="font-mono text-[10px] uppercase tracking-[0.12em] text-[#c75b4a]">coral = no source match</span></div><div className="flex flex-col justify-between gap-5 border-b border-[#d8d5c8] pb-8 lg:flex-row lg:items-end"><div><div className="eyebrow text-[#c75b4a]">03 / Occupations, tasks, and work</div><h2 className="mt-4 max-w-3xl font-display text-4xl leading-[0.98] tracking-[-0.03em] sm:text-5xl">Follow a work record all the way down to what someone actually does.</h2><p className="mt-5 max-w-2xl text-[1.03rem] leading-7 text-[#64655c]">The profile below is assembled from O*NET task, skill, activity, software, and alternate-title records, with BLS wage and employment metrics joined by SOC code where available.</p></div><Badge tone="ochre"><Database className="mr-2 h-3 w-3" /> {integer(data.manifest.counts.occupations)} official occupations</Badge></div><div className="mt-8 grid gap-5 lg:grid-cols-[280px_1fr]"><aside className="overflow-hidden rounded-2xl border border-[#d1cec2] bg-[#fbfaf5]"><div className="border-b border-[#dad7cc] bg-[#f0ede3] p-4"><div className="coordinate-stamp">OCCUPATION DIRECTORY</div><div className="relative mt-3"><Search className="absolute left-3 top-2.5 h-4 w-4 text-[#8a897f]" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search occupation or code" className="w-full rounded-xl border border-[#c9c6ba] bg-[#fbfaf5] py-2 pl-9 pr-3 text-xs outline-none focus:ring-2 focus:ring-[#0f766e]" /></div></div><div className="max-h-[560px] overflow-auto p-2">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item)} className={`flex w-full items-start gap-2 rounded-xl px-3 py-2.5 text-left hover:bg-[#f0ede3] ${selected.id === item.id ? "bg-[#e0f0eb]" : ""}`}><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${item.laborMarket ? "bg-[#0f766e]" : "bg-[#c75b4a]"}`} /><span className="min-w-0"><span className="block truncate text-xs font-medium text-[#41433c]">{item.title}</span><span className="block font-mono text-[10px] text-[#8a897f]">{item.id}</span></span></button>)}</div></aside><OccupationDetail occupation={selected} /></div><div className="mt-8 rounded-2xl border border-[#d1cec2] bg-[#fbfaf5] p-5 sm:p-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="eyebrow text-[#0f766e]">04 / Real-data scatter</div><h3 className="mt-2 font-display text-3xl">Task volume × median annual wage</h3><p className="mt-3 text-sm leading-6 text-[#6b6c64]">Each point is an O*NET occupation joined to the BLS May 2025 cross-industry record when both sources share a detailed SOC code. Point size is fixed for legibility; no synthetic growth metric is shown.</p></div><Badge tone="ink"><Target className="mr-2 h-3 w-3" /> {integer(scatter.length)} joined records</Badge></div><div className="relative mt-8 h-[390px] overflow-hidden border border-[#d4d0c4] bg-[#f3f0e5]"><div className="absolute inset-x-8 bottom-10 border-t border-[#aaa99f]" /><div className="absolute bottom-3 left-1/2 -translate-x-1/2 font-mono text-[10px] uppercase tracking-[0.15em] text-[#84847b]">task statements →</div><div className="absolute bottom-1/2 left-4 -rotate-90 font-mono text-[10px] uppercase tracking-[0.15em] text-[#84847b]">median annual wage →</div>{scatter.map((item, index) => { const wage = item.laborMarket?.medianAnnualWage ?? 0; const x = Math.min(95, 8 + Math.sqrt(item.metrics.taskCount / 120) * 17 + (index % 7)); const y = Math.min(90, 8 + Math.min(75, wage / 1800)); return <button key={item.id} title={`${item.title} · ${money(wage)}`} onClick={() => setSelected(item)} style={{ left: `${x}%`, bottom: `${y}%` }} className={`absolute h-2.5 w-2.5 rounded-full border border-[#fbfaf5] ${selected.id === item.id ? "scale-150 bg-[#c75b4a]" : "bg-[#0f766e]"}`} />; })}</div></div></div></section>; }

function RelationshipViews({ occupation }: { occupation: Occupation }) { const terms = occupation.skills.slice(0, 12); return <section className="container py-16 lg:py-24"><div className="grid gap-8 lg:grid-cols-[.7fr_1.3fr]"><div><div className="eyebrow text-[#0f766e]">05 / Relationship views</div><h2 className="mt-4 font-display text-4xl leading-[0.98] tracking-[-0.03em] sm:text-5xl">Spatial views for the records that already exist.</h2><p className="mt-5 text-[1.03rem] leading-7 text-[#64655c]">These views are honest visual transformations of the selected O*NET record. They do not claim that proximity proves causality.</p><div className="mt-7 flex flex-wrap gap-2"><Badge>O*NET task</Badge><Badge tone="ochre">O*NET skill</Badge><Badge tone="coral">Derived layout</Badge></div></div><div className="grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-[#d1cec2] bg-[#fbfaf5] p-5"><div className="flex items-center gap-2"><NetworkIcon /><div className="coordinate-stamp">SEMANTIC MAP</div></div><div className="relative mt-5 h-64 border border-[#dedbd0] bg-[#f3f0e5]"><div className="absolute left-1/2 top-1/2 grid h-24 w-24 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full border-2 border-[#0f766e] bg-[#e0f0eb] p-2 text-center font-display text-lg leading-tight text-[#0f625b]">{occupation.title}</div>{terms.slice(0, 6).map((skill, index) => { const angle = (index / 6) * Math.PI * 2; const left = 50 + Math.cos(angle) * 35; const top = 50 + Math.sin(angle) * 35; return <div key={skill.name} className="absolute max-w-[90px] -translate-x-1/2 -translate-y-1/2 border border-[#dfc681] bg-[#f6ebc9] px-2 py-1 text-center font-mono text-[9px] leading-3 text-[#8a6417]" style={{ left: `${left}%`, top: `${top}%` }}>{skill.name}</div>; })}</div><p className="mt-4 text-xs leading-5 text-[#77766d]">Center is the source occupation; surrounding labels are its highest recorded essential skills.</p></div><div className="rounded-2xl border border-[#d1cec2] bg-[#fbfaf5] p-5"><div className="flex items-center gap-2"><Sparkles className="h-4 w-4 text-[#c75b4a]" /><div className="coordinate-stamp">WEIGHTED TERMS</div></div><div className="flex min-h-64 flex-wrap content-center items-center justify-center gap-x-5 gap-y-4 py-5 text-center">{terms.map((skill, index) => <span key={skill.name} style={{ fontSize: `${Math.max(12, Math.min(28, 10 + (skill.importance ?? 0) * 4))}px` }} className={`${index % 4 === 0 ? "text-[#0f766e]" : index % 3 === 0 ? "text-[#c75b4a]" : "text-[#55584f]"} font-display`}>{skill.name}</span>)}</div><p className="border-t border-[#dedbd0] pt-4 text-xs leading-5 text-[#77766d]">Font size follows O*NET essential-skill importance; color is a visual key, not a score.</p></div></div></div></section>; }

function Sources({ data }: { data: Release }) { return <section id="sources" className="border-t border-[#d8d5c8] bg-[#242822] text-[#f6f1e6]"><div className="container py-16 lg:py-24"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><div className="eyebrow text-[#9acdc1]">06 / Evidence register</div><h2 className="mt-4 font-display text-4xl leading-[0.98] tracking-[-0.03em] sm:text-5xl">Every number has a source vintage.</h2><p className="mt-5 max-w-2xl text-[1.03rem] leading-7 text-[#c8c8bd]">The release is reproducible from the files listed below. Hashes are recorded in the manifest so an update can prove exactly which source package it used.</p></div><Badge tone="ochre"><Check className="mr-2 h-3 w-3" /> synthetic records: {data.manifest.integrity.syntheticRecords}</Badge></div><div className="mt-8 overflow-hidden border border-white/10"><div className="hidden grid-cols-[1.1fr_1fr_1fr_auto] gap-4 border-b border-white/10 bg-white/5 px-5 py-3 font-mono text-[10px] uppercase tracking-[0.15em] text-[#9acdc1] md:grid"><div>Publisher</div><div>Release</div><div>License / note</div><div>Source</div></div>{data.manifest.sources.map((source) => <div key={source.id} className="grid gap-2 border-b border-white/10 px-5 py-4 last:border-b-0 md:grid-cols-[1.1fr_1fr_1fr_auto] md:items-center md:gap-4"><div className="font-medium">{source.publisher}</div><div className="text-sm text-[#c8c8bd]">{source.title}<span className="block font-mono text-[10px] text-[#9acdc1]">{source.vintage}</span></div><div className="text-xs leading-5 text-[#b8b9ae]">{source.license}</div><a href={source.url} target="_blank" rel="noreferrer" className="text-[#9acdc1] hover:text-[#edb6a7]"><ExternalLink className="h-4 w-4" /></a></div>)}</div><div className="mt-8 border-t border-white/10 pt-6 text-sm leading-7 text-[#b8b9ae]"><strong className="text-[#f6f1e6]">Integrity boundary.</strong> Industry trees and occupation/work data remain separate because the first release contains no authoritative universal industry-to-occupation parent-child crosswalk. The interface will not infer one. Work-rhythm percentages are intentionally absent; the site shows actual task and activity records instead.</div></div></section>; }

function NetworkIcon() { return <GitBranch className="h-4 w-4 text-[#0f766e]" />; }

function AtlasShell({ data }: { data: Release }) {
  const [tab, setTab] = useState<"graph" | "world" | "directory" | "roadmaps">(() => new URLSearchParams(window.location.hash.replace(/^#/, "")).get("world") === "1" ? "world" : "graph");
  const [selectedOccupation, setSelectedOccupation] = useState(data.occupations[0]);
  const [roadmapContext, setRoadmapContext] = useState<RoadmapContext | null>(null);
  const openRoadmap = (context: RoadmapContext) => { setRoadmapContext(context); setTab("roadmaps"); };
  return <div id="top" className="min-h-screen bg-[#f8f5ec] text-[#242822]"><header className="sticky top-0 z-50 border-b border-[#d8d5c8] bg-[#f8f5ec]/95 backdrop-blur-xl"><div className="container flex min-h-[68px] items-center justify-between gap-4"><Brand /><div className="flex items-center gap-1 rounded-full border border-[#c9c6ba] bg-[#f0ede3] p-1" role="tablist" aria-label="Atlas views"><button role="tab" aria-selected={tab === "graph"} onClick={() => setTab("graph")} className={`rounded-full px-3 py-2 text-xs font-semibold transition sm:px-4 ${tab === "graph" ? "bg-[#242822] text-white shadow-sm" : "text-[#666960] hover:text-[#242822]"}`}>Graph</button><button role="tab" aria-selected={tab === "world"} onClick={() => setTab("world")} className={`rounded-full px-3 py-2 text-xs font-semibold transition sm:px-4 ${tab === "world" ? "bg-[#ba7a48] text-white shadow-sm" : "text-[#666960] hover:text-[#242822]"}`}>World</button><button role="tab" aria-selected={tab === "roadmaps"} onClick={() => setTab("roadmaps")} className={`rounded-full px-3 py-2 text-xs font-semibold transition sm:px-4 ${tab === "roadmaps" ? "bg-[#b95c78] text-white shadow-sm" : "text-[#666960] hover:text-[#242822]"}`}>Roadmaps</button><button role="tab" aria-selected={tab === "directory"} onClick={() => setTab("directory")} className={`rounded-full px-3 py-2 text-xs font-semibold transition sm:px-4 ${tab === "directory" ? "bg-[#0f766e] text-white shadow-sm" : "text-[#666960] hover:text-[#242822]"}`}>Directory</button></div><div className="hidden items-center gap-2 sm:flex"><Badge>{integer(data.manifest.counts.occupations)} occupations</Badge><span className="font-mono text-[9px] uppercase tracking-[0.14em] text-[#777970]">release {data.manifest.releaseId}</span></div></div></header><main>{tab === "graph" ? <div className="min-h-[calc(100vh-68px)]"><InfiniteAtlas data={data} onOpenRoadmap={openRoadmap} /></div> : tab === "world" ? <GlobalVentureAtlas /> : tab === "roadmaps" ? <AtlasRoadmaps data={data} context={roadmapContext} onReturnToGraph={() => setTab("graph")} /> : <div><div className="container border-b border-[#d8d5c8] py-10"><div className="eyebrow text-[#0f766e]">Directory / source records</div><h1 className="mt-3 max-w-3xl font-display text-5xl leading-[0.92] tracking-[-0.04em] sm:text-6xl">Browse the official folders, then open the work record.</h1><p className="mt-4 max-w-2xl text-base leading-7 text-[#666960]">This view is for deliberate inspection: classification codes, official child records, occupations, tasks, skills, wages, and source links.</p></div><TaxonomyExplorer data={data} /><OccupationExplorer data={data} selected={selectedOccupation} setSelected={setSelectedOccupation} /><Sources data={data} /></div>}</main></div>;
}

export default function RealHome() {
  const [data, setData] = useState<Release>(null as unknown as Release);
  const [error, setError] = useState<string | null>(null);
  const [selectedOccupation, setSelectedOccupation] = useState<Occupation | null>(null);
  useEffect(() => { Promise.all([fetch("/data/manifest.json").then((response) => response.json()), fetch("/data/taxonomies.json").then((response) => response.json()), fetch("/data/occupations.json").then((response) => response.json())]).then(([manifest, taxonomies, occupations]) => setData({ manifest, taxonomies, occupations })).catch(() => setError("The real-data release could not be loaded. Check that the generated data files are present.")); }, []);
  if (error) return <div className="grid min-h-screen place-items-center bg-[#f8f5ec] p-6 text-center"><div><div className="eyebrow text-[#c75b4a]">Data release unavailable</div><h1 className="mt-4 font-display text-4xl">{error}</h1><p className="mt-4 text-sm text-[#686960]">Run <code>python3 scripts/build_real_data.py</code> from the project root.</p></div></div>;
  if (!data) return <div className="grid min-h-screen place-items-center bg-[#f8f5ec]"><div className="animate-pulse font-display text-3xl text-[#0f766e]">Loading the source release…</div></div>;
  return <AtlasShell data={data} />;
  const activeOccupation = selectedOccupation ?? data.occupations[0];
  return <div id="top" className="min-h-screen bg-[#f8f5ec] text-[#242822]"><header className="sticky top-0 z-40 border-b border-[#d8d5c8]/80 bg-[#f8f5ec]/90 backdrop-blur-xl"><div className="container flex h-[70px] items-center justify-between"><Brand /><nav className="hidden items-center gap-6 text-sm text-[#65665d] md:flex"><a href="#atlas-canvas" className="nav-link">Atlas</a><a href="#sources" className="nav-link">Sources</a></nav><div className="flex items-center gap-2"><a href="#atlas-canvas" className="rounded-full bg-[#0f766e] px-4 py-2 text-xs font-semibold text-white transition hover:bg-[#0b625c]">Open atlas <ArrowRight className="ml-1 inline h-3.5 w-3.5" /></a></div></div></header><main><section className="relative overflow-hidden"><div className="absolute inset-0 opacity-25" style={{ backgroundImage: "radial-gradient(circle at 68% 20%, #0f766e 0 1px, transparent 1.5px), radial-gradient(circle at 78% 74%, #c75b4a 0 1px, transparent 1.5px)", backgroundSize: "44px 44px, 76px 76px" }} /><div className="absolute inset-0 bg-gradient-to-r from-[#f8f5ec] via-[#f8f5ec]/95 to-[#f8f5ec]/60" /><div className="container relative grid min-h-[500px] items-center gap-12 py-20 lg:grid-cols-[1.05fr_.95fr] lg:py-28"><div><div className="eyebrow flex items-center gap-2 text-[#0f766e]"><span className="h-px w-8 bg-[#0f766e]" /> Real source release · {data.manifest.releaseId}</div><h1 className="mt-6 max-w-4xl font-display text-[clamp(3.7rem,8vw,7.5rem)] leading-[0.84] tracking-[-0.065em]">A world of work.<br /><em className="text-[#0f766e]">At every scale.</em></h1><p className="mt-8 max-w-xl text-lg leading-8 text-[#5c5e55]">One source-backed map. Scroll to zoom, drag to roam, and click a cluster until an official record becomes a task someone actually performs.</p><div className="mt-9 flex flex-wrap gap-3"><a href="#atlas-canvas" className="rounded-full bg-[#242822] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#0f766e]">Enter the infinite atlas <ArrowRight className="ml-2 inline h-4 w-4" /></a><a href="#sources" className="rounded-full border border-[#bdb9ac] bg-[#f8f5ec]/80 px-5 py-3 text-sm font-semibold text-[#454740] transition hover:border-[#0f766e]">Read the sources</a></div><div className="mt-14 flex flex-wrap gap-2"><Badge>{integer(data.manifest.counts.occupations)} occupations</Badge><Badge tone="ochre">{integer(data.manifest.counts.taskStatementCount)} task statements</Badge><Badge tone="ink">{integer(data.manifest.counts.naics)} NAICS records</Badge><Badge tone="coral">{integer(data.manifest.counts.isic)} ISIC records</Badge></div></div><div className="relative hidden lg:block"><div className="hero-note absolute right-0 top-0 rotate-3"><div className="eyebrow text-[#c75b4a]">Field note / map rules</div><p className="mt-4 font-display text-2xl leading-tight text-[#353831]">Zoom is the first filter.</p><p className="mt-4 text-sm leading-6 text-[#6b6c64]">At world scale you see clusters. At record scale you see names. Search only moves the camera to a place that interests you.</p><div className="mt-6 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.14em] text-[#0f766e]"><Map className="h-4 w-4" /> one canvas / many scales</div></div></div></div></section><InfiniteAtlas data={data} /><Sources data={data} /><section className="border-t border-[#d8d5c8] bg-[#f0ede3]"><div className="container flex flex-col justify-between gap-7 py-12 sm:flex-row sm:items-center"><div><div className="eyebrow text-[#0f766e]">Refresh path</div><p className="mt-3 font-display text-3xl">Download → validate → build → deploy.</p></div><a href="https://www.onetcenter.org/database.html" target="_blank" rel="noreferrer" className="rounded-full bg-[#c75b4a] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#a94c3e]">View source release <ExternalLink className="ml-2 inline h-4 w-4" /></a></div></section></main><footer className="bg-[#242822] text-[#b8b9ae]"><div className="container flex flex-col justify-between gap-4 py-8 text-xs sm:flex-row sm:items-center"><Brand /><div className="flex flex-wrap gap-x-5 gap-y-2"><span>Real source release</span><span>Infinite canvas</span><span>Provenance first</span></div></div></footer></div>;
}
