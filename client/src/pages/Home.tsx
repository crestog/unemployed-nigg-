// Industry Niche Atlas style reminder: editorial cartography, warm mineral paper, ink typography, oxidized teal paths, coral signals, evidence beside interpretation.

import { useMemo, useState } from "react";
import { ArrowDownRight, ArrowLeft, ArrowRight, BookOpen, Check, ChevronDown, Copy, ExternalLink, FileText, Filter, GitBranch, Layers3, Map, Network, Pin, Search, Share2, Sparkles, Table2, Target, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { taxonomyStats, nodes, taskRows, sources, scatterPoints, wordCloud, clusterItems, type AtlasNode } from "@/lib/atlasData";
import RouteMap from "@/components/RouteMap";

type ViewMode = "directory" | "scatter" | "semantic" | "wordcloud" | "cluster";
const viewMeta: Record<ViewMode, { label: string; icon: typeof Map; question: string }> = {
  directory: { label: "Directory", icon: Layers3, question: "Where am I in the hierarchy?" },
  scatter: { label: "Scatter", icon: Target, question: "Which niches combine growth, pay, and density?" },
  semantic: { label: "Semantic map", icon: Network, question: "What concepts connect across systems?" },
  wordcloud: { label: "Word cloud", icon: Sparkles, question: "What language dominates this niche?" },
  cluster: { label: "Cluster map", icon: GitBranch, question: "Which skills and tasks co-occur?" },
};

const toneClasses = {
  teal: "bg-[#d4ebe5] text-[#0f625b] border-[#9acdc1]",
  coral: "bg-[#f8ddd5] text-[#9d483c] border-[#edb6a7]",
  ochre: "bg-[#f4e6bf] text-[#8a6417] border-[#dfc681]",
  ink: "bg-[#e9e7df] text-[#3f423e] border-[#c9c6ba]",
};

function Logo({ small = false }: { small?: boolean }) {
  return <div className="flex items-center gap-3"><img src="/manus-storage/atlas-mark_0cafe457.png" alt="Industry Niche Atlas mark" className={small ? "h-8 w-8" : "h-10 w-10"} /><div className={small ? "hidden sm:block" : ""}><div className="font-display text-[1.05rem] leading-none text-[#242822]">Industry Niche</div><div className="font-display text-[1.05rem] italic leading-none text-[#0f766e]">Atlas</div></div></div>;
}

function StatCard({ stat }: { stat: (typeof taxonomyStats)[number] }) {
  return <div className="stat-card"><div className={`eyebrow text-${stat.accent === "teal" ? "teal" : "stone"}-700`}>{stat.label}</div><div className="mt-3 font-display text-3xl text-[#242822]">{stat.value}</div><div className="mt-1 text-xs text-[#77766d]">{stat.note}</div></div>;
}

function LevelBadge({ node }: { node: AtlasNode }) {
  return <span className={`rounded-full border px-2 py-1 font-mono text-[10px] uppercase tracking-[0.16em] ${toneClasses[node.tone]}`}>L{node.level} · {node.type}</span>;
}

function Explorer({ selected, setSelected }: { selected: AtlasNode; setSelected: (node: AtlasNode) => void }) {
  const [view, setView] = useState<ViewMode>("directory");
  const [metric, setMetric] = useState("growth");
  const [query, setQuery] = useState("");
  const [pinned, setPinned] = useState<string[]>([]);
  const filteredNodes = useMemo(() => nodes.filter((node) => `${node.label} ${node.type} ${node.code}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const currentIndex = nodes.findIndex((node) => node.id === selected.id);
  const nextNode = nodes[Math.min(currentIndex + 1, nodes.length - 1)];

  const selectNode = (node: AtlasNode) => setSelected(node);
  const sharePath = async () => {
    const text = `Industry Niche Atlas · ${nodes.slice(0, currentIndex + 1).map((node) => node.label).join(" → ")}`;
    try { await navigator.clipboard.writeText(text); } catch { /* Clipboard is optional in a static preview. */ }
  };

  return <section id="explorer" className="atlas-section border-y border-[#d8d5c8] bg-[#f5f1e7]">
    <div className="container py-14 lg:py-20">
      <div className="flex flex-col justify-between gap-5 border-b border-[#d8d5c8] pb-8 lg:flex-row lg:items-end"><div><div className="eyebrow text-[#0f766e]">02 / Explorer workspace</div><h2 className="mt-4 max-w-3xl font-display text-4xl leading-[0.98] tracking-[-0.03em] text-[#242822] sm:text-5xl">Click any node. Keep the thread.</h2><p className="mt-5 max-w-2xl text-[1.03rem] leading-7 text-[#64655c]">The prototype treats industry, occupation, skill, and task as connected layers—not one falsely tidy tree.</p></div><div className="flex gap-2"><Button variant="outline" className="border-[#b8b5a8] bg-[#f5f1e7] text-[#353831]" onClick={sharePath}><Share2 className="mr-2 h-4 w-4" /> Share path</Button><Button className="bg-[#0f766e] text-white hover:bg-[#0b625c]" onClick={() => setPinned((items) => items.includes(selected.id) ? items.filter((item) => item !== selected.id) : [...items, selected.id])}><Pin className="mr-2 h-4 w-4" /> {pinned.includes(selected.id) ? "Pinned" : "Pin node"}</Button></div></div>
      <div className="mt-8 overflow-hidden rounded-[1.75rem] border border-[#c9c6ba] bg-[#fbfaf5] shadow-[0_18px_55px_rgba(52,57,48,0.08)]">
        <div className="flex flex-col border-b border-[#dad7cc] bg-[#f1eee4] lg:flex-row"><div className="flex items-center gap-2 overflow-x-auto px-4 py-3 lg:w-[66%]">{(Object.keys(viewMeta) as ViewMode[]).map((key) => { const Item = viewMeta[key].icon; return <button key={key} onClick={() => setView(key)} className={`view-tab ${view === key ? "view-tab-active" : ""}`}><Item className="h-3.5 w-3.5" />{viewMeta[key].label}</button>; })}</div><div className="flex items-center gap-2 border-t border-[#dad7cc] px-4 py-3 text-xs text-[#73746b] lg:w-[34%] lg:border-l lg:border-t-0"><span className="font-mono text-[10px] uppercase tracking-[0.15em] text-[#0f766e]">Lens</span><select aria-label="Choose metric" value={metric} onChange={(event) => setMetric(event.target.value)} className="bg-transparent font-medium text-[#41433c] outline-none"><option value="growth">Projected growth</option><option value="pay">Median pay</option><option value="density">Employment density</option></select><Filter className="ml-auto h-4 w-4" /></div></div>
        <div className="grid min-h-[620px] lg:grid-cols-[250px_1fr_310px]">
          <aside className="border-b border-[#dad7cc] bg-[#f0ede3] p-4 lg:border-b-0 lg:border-r"><div className="eyebrow text-[#77766d]">Lineage / 05</div><div className="mt-4 flex flex-wrap gap-1.5">{nodes.slice(0, currentIndex + 1).map((node, index) => <button key={node.id} onClick={() => selectNode(node)} className={`lineage-pill ${index === currentIndex ? "lineage-current" : ""}`}>{node.label}<ChevronDown className="ml-1 h-3 w-3 rotate-[-90deg]" /></button>)}</div><div className="mt-8 flex items-center gap-2 border-y border-[#d7d3c6] py-3 text-xs text-[#74756d]"><span className="h-2 w-2 rounded-full bg-[#0f766e]" /> {currentIndex + 1} layers mapped</div><div className="mt-5 space-y-2">{filteredNodes.map((node) => <button key={node.id} onClick={() => selectNode(node)} className={`node-list-item ${selected.id === node.id ? "node-list-active" : ""}`}><span className={`h-2 w-2 shrink-0 rounded-full ${node.tone === "teal" ? "bg-[#0f766e]" : node.tone === "coral" ? "bg-[#c75b4a]" : node.tone === "ochre" ? "bg-[#ba8a2d]" : "bg-[#55584f]"}`} /><span className="min-w-0 text-left"><span className="block truncate text-sm font-medium">{node.label}</span><span className="block font-mono text-[10px] text-[#8a897f]">{node.code}</span></span></button>)}</div><div className="mt-8"><div className="eyebrow text-[#77766d]">Jump to a node</div><div className="relative mt-2"><Search className="absolute left-3 top-2.5 h-4 w-4 text-[#8a897f]" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search labels or codes" className="w-full rounded-xl border border-[#c9c6ba] bg-[#fbfaf5] py-2 pl-9 pr-3 text-xs outline-none ring-[#0f766e] focus:ring-2" /></div></div></aside>
          <main className="relative min-w-0 overflow-hidden p-5 sm:p-8"><div className="flex items-start justify-between gap-4"><div><div className="eyebrow text-[#77766d]">{viewMeta[view].question}</div><h3 className="mt-2 font-display text-2xl text-[#242822]">{selected.label}</h3></div><div className="rounded-full border border-[#d6d2c6] bg-white/60 px-3 py-1.5 font-mono text-[10px] uppercase tracking-[0.16em] text-[#77766d]">{metric} / demo layer</div></div>
            {view === "directory" && <><RouteMap /><DirectoryView selected={selected} selectNode={selectNode} nextNode={nextNode} /></>}
            {view === "scatter" && <ScatterView selectNode={selectNode} />}
            {view === "semantic" && <SemanticView selectNode={selectNode} />}
            {view === "wordcloud" && <WordCloudView selectNode={selectNode} />}
            {view === "cluster" && <ClusterView />}
          </main>
          <aside className="border-t border-[#dad7cc] bg-[#f7f4eb] p-5 lg:border-l lg:border-t-0 lg:p-6"><div className="flex items-center justify-between"><div className="eyebrow text-[#0f766e]">Evidence drawer</div><button aria-label="Close evidence drawer" className="rounded-full p-1.5 text-[#77766d] hover:bg-[#ece8dd]"><X className="h-4 w-4" /></button></div><div className="mt-5"><LevelBadge node={selected} /><h4 className="mt-4 font-display text-2xl leading-tight text-[#242822]">{selected.label}</h4><p className="mt-3 text-sm leading-6 text-[#686960]">{selected.description}</p></div><div className="mt-6 grid grid-cols-2 gap-2"><div className="mini-stat"><div className="eyebrow text-[#77766d]">Node code</div><div className="mt-2 font-mono text-xs text-[#353831]">{selected.code}</div></div><div className="mini-stat"><div className="eyebrow text-[#77766d]">Mapped signal</div><div className="mt-2 text-xs font-medium text-[#353831]">{selected.count}</div></div></div><div className="mt-6 border-t border-[#dad7cc] pt-5"><div className="eyebrow text-[#77766d]">What this means</div><p className="mt-2 text-sm leading-6 text-[#52544c]">This node is a doorway, not a dead end. Explore the next relationship to see where the work becomes more specific.</p></div><div className="mt-6 border-t border-[#dad7cc] pt-5"><div className="eyebrow text-[#77766d]">Sources nearby</div><a href="https://www.onetcenter.org/database.html" target="_blank" rel="noreferrer" className="source-link mt-3">O*NET 30.3 database <ExternalLink className="h-3.5 w-3.5" /></a><a href="https://www.spglobal.com/spdji/en/landing/topic/gics/" target="_blank" rel="noreferrer" className="source-link">GICS framework <ExternalLink className="h-3.5 w-3.5" /></a></div><Button className="mt-7 w-full bg-[#242822] text-white hover:bg-[#0f766e]" onClick={() => selectNode(nextNode)} disabled={selected.id === nextNode.id}>Go one level deeper <ArrowRight className="ml-2 h-4 w-4" /></Button></aside>
        </div>
      </div>
    </div>
  </section>;
}

function DirectoryView({ selected, selectNode, nextNode }: { selected: AtlasNode; selectNode: (node: AtlasNode) => void; nextNode: AtlasNode }) {
  return <div className="mt-8"><div className="atlas-route"><div className="route-line" />{nodes.map((node, index) => <button key={node.id} onClick={() => selectNode(node)} className={`route-node ${node.level <= selected.level ? "route-node-seen" : ""} ${node.id === selected.id ? "route-node-current" : ""}`}><span className="route-index">0{index + 1}</span><span className="mt-2 block font-display text-lg leading-tight text-[#242822]">{node.label}</span><span className="mt-2 block font-mono text-[10px] uppercase tracking-[0.12em] text-[#77766d]">{node.type}</span></button>)}</div><div className="mt-10 grid gap-4 md:grid-cols-2"><button onClick={() => selectNode(nextNode)} className="explore-card group text-left"><div className="flex items-center justify-between"><span className="eyebrow text-[#0f766e]">Next relationship</span><ArrowDownRight className="h-5 w-5 text-[#0f766e] transition-transform group-hover:translate-x-1 group-hover:translate-y-1" /></div><h4 className="mt-5 font-display text-2xl text-[#242822]">{selected.id === nextNode.id ? "You reached the edge of this demo" : `Follow into ${nextNode.label}`}</h4><p className="mt-2 text-sm leading-6 text-[#6b6c64]">{selected.id === nextNode.id ? "The production atlas would continue with a source-backed specialty or work profile." : nextNode.description}</p></button><div className="explore-card"><div className="eyebrow text-[#77766d]">Model boundary</div><h4 className="mt-5 font-display text-2xl text-[#242822]">Tree first. Graph next.</h4><p className="mt-2 text-sm leading-6 text-[#6b6c64]">Formal classifications stop at an industry or occupation code. The atlas then pivots to typed links for skills, tasks, and context.</p></div></div></div>;
}

function ScatterView({ selectNode }: { selectNode: (node: AtlasNode) => void }) { return <div className="mt-8"><div className="relative h-[360px] rounded-2xl border border-[#d4d0c4] bg-[#f3f0e5] p-6"><div className="absolute inset-x-8 bottom-10 border-t border-[#aaa99f]" /><div className="absolute bottom-3 left-1/2 -translate-x-1/2 font-mono text-[10px] uppercase tracking-[0.15em] text-[#84847b]">Projected change →</div><div className="absolute bottom-1/2 left-4 -rotate-90 font-mono text-[10px] uppercase tracking-[0.15em] text-[#84847b]">Median earning signal →</div>{scatterPoints.map((point) => <button key={point.label} onClick={() => selectNode(nodes.find((node) => node.label.toLowerCase().includes(point.label.split(" ")[0].toLowerCase())) ?? nodes[2])} style={{ left: `${point.x}%`, bottom: `${point.y}%`, width: point.size, height: point.size }} className={`scatter-point ${point.tone}`} aria-label={point.label}><span className="scatter-tooltip">{point.label}</span></button>)}</div><div className="mt-5 flex flex-wrap gap-2 text-xs text-[#717269]"><span className="legend-dot bg-[#0f766e]" /> labor-market signal <span className="legend-dot bg-[#c75b4a]" /> emerging niche <span className="legend-dot bg-[#ba8a2d]" /> context layer</div></div>; }

function SemanticView({ selectNode }: { selectNode: (node: AtlasNode) => void }) { return <div className="mt-8"><div className="relative h-[360px] overflow-hidden rounded-2xl border border-[#d4d0c4] bg-[#f3f0e5] network-canvas"><svg viewBox="0 0 700 350" className="absolute inset-0 h-full w-full"><path d="M80 238 C180 130 190 160 290 95 S430 82 525 150 S600 240 625 285" fill="none" stroke="#9bbfb7" strokeWidth="2" strokeDasharray="5 7" /><path d="M112 110 C220 190 260 250 410 250 S540 180 590 100" fill="none" stroke="#e3b0a3" strokeWidth="2" strokeDasharray="4 8" /></svg>{[[12,62,"Industry","information","teal"],[30,27,"Domain","data","coral"],[45,70,"Occupation","scientist","ochre"],[64,24,"Task","clinical","teal"],[80,61,"Skill","clinical","coral"],[88,27,"Tool","scientist","ink"]].map(([left, top, label, id, tone]) => <button key={`${label}-${left}`} onClick={() => selectNode(nodes.find((node) => node.id === id) ?? nodes[2])} style={{ left: `${left}%`, top: `${top}%` }} className={`semantic-node ${tone}`}><span className="font-mono text-[9px] uppercase tracking-[0.16em]">{label}</span><strong>{nodes.find((node) => node.id === id)?.label ?? "Connected node"}</strong></button>)}</div><p className="mt-4 text-sm leading-6 text-[#6b6c64]">Distance is a visual aid, not a claim of causality. Typed edges and source links carry the meaning.</p></div>; }

function WordCloudView({ selectNode }: { selectNode: (node: AtlasNode) => void }) { return <div className="mt-8 rounded-2xl border border-[#d4d0c4] bg-[#f3f0e5] p-8"><div className="flex min-h-[300px] flex-wrap content-center justify-center gap-x-6 gap-y-5 text-center">{wordCloud.map(([word, weight], index) => <button key={word} onClick={() => selectNode(index % 3 === 0 ? nodes[2] : nodes[4])} style={{ fontSize: `${12 + weight / 3}px`, transform: `rotate(${index % 3 === 0 ? -4 : index % 3 === 1 ? 2 : 0}deg)` }} className={`font-display transition-transform hover:scale-110 ${index % 4 === 0 ? "text-[#0f766e]" : index % 3 === 0 ? "text-[#c75b4a]" : "text-[#5a5b53]"}`}>{word}</button>)}</div><div className="border-t border-[#d4d0c4] pt-4 text-center font-mono text-[10px] uppercase tracking-[0.15em] text-[#85847a]">weighted terms · prototype sample · click a term to follow the thread</div></div>; }

function ClusterView() { return <div className="mt-8 grid gap-4 md:grid-cols-3">{clusterItems.map((cluster) => <div key={cluster.title} className={`cluster-card ${cluster.tone}`}><div className="eyebrow">Cluster 0{clusterItems.indexOf(cluster) + 1}</div><h4 className="mt-4 font-display text-2xl text-[#242822]">{cluster.title}</h4><div className="mt-6 space-y-2">{cluster.items.map((item) => <div key={item} className="rounded-xl border border-[#d7d3c6] bg-white/50 px-3 py-2 text-sm text-[#56584f]">{item}</div>)}</div></div>)}</div>; }

function WorkProfile({ selected }: { selected: AtlasNode }) { const [scale, setScale] = useState<"day" | "week" | "month">("day"); return <section className="container py-16 lg:py-24"><div className="grid gap-10 lg:grid-cols-[0.85fr_1.15fr] lg:items-start"><div><div className="eyebrow text-[#c75b4a]">03 / Work profile</div><h2 className="mt-4 font-display text-4xl leading-[0.98] tracking-[-0.03em] text-[#242822] sm:text-5xl">Where the taxonomy ends, the week begins.</h2><p className="mt-5 max-w-md text-[1.02rem] leading-7 text-[#66675f]">A niche is useful only when it becomes concrete: tasks, tools, handoffs, constraints, and the rhythm of a real working life.</p><div className="mt-8 rounded-2xl border border-[#d8d5c8] bg-[#f6f1e6] p-5"><div className="flex items-center gap-3"><FileText className="h-5 w-5 text-[#0f766e]" /><div><div className="eyebrow text-[#77766d]">Prototype profile</div><div className="mt-1 font-display text-xl text-[#242822]">{selected.label}</div></div></div><p className="mt-4 text-sm leading-6 text-[#686960]">Illustrative synthesis. Production data should combine O*NET tasks, work context, professional sources, and reviewed diaries.</p></div></div><div className="rounded-[1.75rem] border border-[#c9c6ba] bg-[#fbfaf5] p-5 shadow-[0_18px_55px_rgba(52,57,48,0.06)] sm:p-7"><div className="flex flex-col justify-between gap-4 border-b border-[#dad7cc] pb-5 sm:flex-row sm:items-center"><div><div className="eyebrow text-[#77766d]">A time-scale view</div><h3 className="mt-2 font-display text-2xl text-[#242822]">An analyst’s working rhythm</h3></div><div className="flex rounded-xl border border-[#d0cdc1] bg-[#f2efe5] p-1">{(["day", "week", "month"] as const).map((item) => <button key={item} onClick={() => setScale(item)} className={`rounded-lg px-3 py-1.5 text-xs font-medium capitalize ${scale === item ? "bg-white text-[#0f766e] shadow-sm" : "text-[#77766d]"}`}>{item}</button>)}</div></div><div className="mt-7 space-y-5">{taskRows.map((row) => { const value = row[scale]; return <div key={row.label}><div className="mb-2 flex justify-between text-sm"><span className="font-medium text-[#454740]">{row.label}</span><span className="font-mono text-xs text-[#7e7e74]">{value}% signal</span></div><div className="h-3 rounded-full bg-[#e9e6dc]"><div style={{ width: `${value * 2.5}%` }} className={`h-3 rounded-full ${row.tone === "teal" ? "bg-[#5ca99d]" : row.tone === "coral" ? "bg-[#d58d7e]" : row.tone === "ochre" ? "bg-[#d4b56a]" : "bg-[#787a70]"}`} /></div></div>; })}</div><div className="mt-8 flex flex-wrap gap-2 border-t border-[#dad7cc] pt-5"><Badge className="border-[#b7d8d1] bg-[#e0f0eb] text-[#0f625b]">Mostly digital</Badge><Badge className="border-[#edc1b6] bg-[#fae4de] text-[#9d483c]">High documentation</Badge><Badge className="border-[#dfc681] bg-[#f6ebc9] text-[#8a6417]">Cross-functional</Badge></div></div></div></section>; }

function ResearchResults() { return <section id="research" className="border-t border-[#d8d5c8] bg-[#242822] text-[#f6f1e6]"><div className="container py-16 lg:py-24"><div className="grid gap-10 lg:grid-cols-[0.8fr_1.2fr] lg:items-start"><div><div className="eyebrow text-[#9acdc1]">04 / Research findings</div><h2 className="mt-4 font-display text-4xl leading-[0.98] tracking-[-0.03em] sm:text-5xl">The answer is a family of lenses.</h2><p className="mt-5 max-w-md text-[1.02rem] leading-7 text-[#c8c8bd]">Official systems are excellent at counting and comparing. None of them alone explains the full lived reality of work. The atlas joins them without collapsing their differences.</p><div className="mt-8 overflow-hidden rounded-2xl border border-white/10 bg-white/5"><img src="/manus-storage/atlas-field-notes_f13c6a02.jpg" alt="Editorial field notes and taxonomy sketches" className="h-56 w-full object-cover opacity-85" /></div></div><div className="space-y-4">{[["01","A formal tree", "GICS, NAICS, ISIC, and NACE provide the economic activity spine. Their counts differ because their purposes differ."],["02","A work graph", "O*NET, ESCO, and ISCO connect occupations, skills, activities, and contexts. These are relationships, not one clean parent-child tree."],["03","A lived profile", "Tasks, tools, cadence, deliverables, and constraints make a narrow niche recognizable to a person—not just to a database."],["04","An evidence trail", "Every node needs a publisher, vintage, license, link, and caveat. Uncertainty is part of the interface, not a footnote."]].map(([number,title,body]) => <div key={number} className="research-row"><span className="font-mono text-xs text-[#9acdc1]">{number}</span><div><h3 className="font-display text-2xl">{title}</h3><p className="mt-2 text-sm leading-6 text-[#b8b9ae]">{body}</p></div></div>)}</div></div></div></section>; }

function SourcesSection() { return <section id="sources" className="container py-16 lg:py-24"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><div className="eyebrow text-[#0f766e]">05 / Evidence register</div><h2 className="mt-4 font-display text-4xl leading-[0.98] tracking-[-0.03em] text-[#242822] sm:text-5xl">Start with sources that can survive a question.</h2></div><a href="/industry-niche-atlas-plan.md" className="inline-flex items-center gap-2 text-sm font-semibold text-[#0f766e] hover:underline">Open full plan <ArrowRight className="h-4 w-4" /></a></div><div className="mt-8 overflow-hidden rounded-2xl border border-[#d1cec2] bg-[#fbfaf5]"><div className="hidden grid-cols-[1fr_1fr_1fr_auto] gap-4 border-b border-[#dad7cc] bg-[#f0ede3] px-5 py-3 font-mono text-[10px] uppercase tracking-[0.15em] text-[#77766d] md:grid"><div>Publisher</div><div>Evidence</div><div>Role</div><div>Link</div></div>{sources.map((source) => <div key={source.name} className="grid gap-2 border-b border-[#e2dfd4] px-5 py-4 last:border-b-0 md:grid-cols-[1fr_1fr_1fr_auto] md:items-center md:gap-4"><div className="font-medium text-[#353831]">{source.name}</div><div className="text-sm text-[#5f6158]">{source.item}</div><div className="text-xs uppercase tracking-[0.12em] text-[#8a897f]">{source.kind}</div><a href={source.url} target="_blank" rel="noreferrer" aria-label={`Open ${source.name}`} className="text-[#0f766e] hover:text-[#c75b4a]"><ExternalLink className="h-4 w-4" /></a></div>)}</div></section>; }

export default function Home() {
  const [selected, setSelected] = useState<AtlasNode>(nodes[0]);
  const [copied, setCopied] = useState(false);
  const copyPlan = async () => { try { await navigator.clipboard.writeText("Industry Niche Atlas — full research and implementation plan available in the project files."); setCopied(true); setTimeout(() => setCopied(false), 1800); } catch { setCopied(true); } };
  return <div className="min-h-screen bg-[#f8f5ec] text-[#242822]"><header className="sticky top-0 z-40 border-b border-[#d8d5c8]/80 bg-[#f8f5ec]/90 backdrop-blur-xl"><div className="container flex h-[70px] items-center justify-between"><Logo /><nav className="hidden items-center gap-6 text-sm text-[#65665d] md:flex"><a href="#explorer" className="nav-link">Explore</a><a href="#research" className="nav-link">Research</a><a href="#sources" className="nav-link">Sources</a></nav><div className="flex items-center gap-2"><button onClick={copyPlan} className="hidden rounded-full border border-[#c9c6ba] bg-[#fbfaf5] px-3 py-2 text-xs font-semibold text-[#454740] transition hover:border-[#0f766e] sm:flex sm:items-center sm:gap-2"><Copy className="h-3.5 w-3.5" /> {copied ? "Copied" : "Copy plan"}</button><a href="#explorer" className="rounded-full bg-[#0f766e] px-4 py-2 text-xs font-semibold text-white transition hover:bg-[#0b625c]">Open atlas <ArrowRight className="ml-1 inline h-3.5 w-3.5" /></a></div></div></header><main><section className="relative overflow-hidden"><div className="absolute inset-0 bg-[url('/manus-storage/atlas-hero-map_f71cacd0.jpg')] bg-cover bg-center opacity-45" /><div className="absolute inset-0 bg-gradient-to-r from-[#f8f5ec] via-[#f8f5ec]/95 to-[#f8f5ec]/45" /><div className="container relative grid min-h-[620px] items-center gap-12 py-20 lg:grid-cols-[1.1fr_0.9fr] lg:py-28"><div className="max-w-3xl"><div className="eyebrow flex items-center gap-2 text-[#0f766e]"><span className="h-px w-8 bg-[#0f766e]" /> Research prototype · v0.1</div><h1 className="mt-6 max-w-4xl font-display text-[clamp(3.7rem,8vw,7.5rem)] leading-[0.84] tracking-[-0.065em] text-[#242822]">Start broad.<br /><em className="text-[#0f766e]">Follow the work.</em></h1><p className="mt-8 max-w-xl text-lg leading-8 text-[#5c5e55]">A living atlas for moving from industries to sub-industries, professions, tasks, and the narrow work niches hiding inside them.</p><div className="mt-9 flex flex-wrap gap-3"><a href="#explorer" className="rounded-full bg-[#242822] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#0f766e]">Choose an industry <ArrowRight className="ml-2 inline h-4 w-4" /></a><a href="#research" className="rounded-full border border-[#bdb9ac] bg-[#f8f5ec]/80 px-5 py-3 text-sm font-semibold text-[#454740] transition hover:border-[#0f766e]">Read the research</a></div><div className="mt-14 flex flex-wrap items-center gap-x-7 gap-y-3 text-xs text-[#77766d]"><span className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-[#0f766e]" /> 4 taxonomy lenses</span><span className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-[#c75b4a]" /> 11 exploration layers</span><span className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-[#ba8a2d]" /> source-backed</span></div></div><div className="relative hidden lg:block"><div className="hero-note absolute -right-2 top-2 rotate-3"><div className="eyebrow text-[#c75b4a]">Field note / 001</div><p className="mt-4 font-display text-2xl leading-tight text-[#353831]">“How many industries?” is the wrong first question.</p><p className="mt-4 text-sm leading-6 text-[#6b6c64]">The useful question is: which lens are we using, and what kind of work do we want to see?</p><div className="mt-6 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.14em] text-[#0f766e]"><Map className="h-4 w-4" /> mark your route</div></div><div className="absolute -bottom-24 -left-8 rounded-2xl border border-[#bfc9c0] bg-[#e0f0eb]/80 px-4 py-3 text-xs text-[#236d65] shadow-sm backdrop-blur"><div className="font-mono text-[10px] uppercase tracking-[0.14em]">Current route</div><div className="mt-1 font-medium">Sector → domain → work profile</div></div></div></div></section><section className="container pb-16 pt-2 lg:pb-24"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{taxonomyStats.map((stat) => <StatCard key={stat.label} stat={stat} />)}</div><div className="mt-8 grid gap-8 border-t border-[#d8d5c8] pt-8 lg:grid-cols-[1fr_2fr]"><div><div className="eyebrow text-[#77766d]">The count problem</div><h2 className="mt-3 font-display text-3xl leading-tight text-[#242822]">There is no single world industry list.</h2></div><p className="max-w-3xl text-[1.03rem] leading-7 text-[#63645b]">GICS counts market sectors. NAICS counts business establishments. ISIC counts economic activities. O*NET counts occupations and tasks. The atlas keeps these systems side by side, then lets you follow their edges.</p></div></section><Explorer selected={selected} setSelected={setSelected} /><WorkProfile selected={selected} /><ResearchResults /><SourcesSection /><section className="border-t border-[#d8d5c8] bg-[#f0ede3]"><div className="container flex flex-col justify-between gap-7 py-12 sm:flex-row sm:items-center"><div><div className="eyebrow text-[#0f766e]">Ready for the next layer?</div><p className="mt-3 font-display text-3xl text-[#242822]">Build the data spine. Then let curiosity do the navigating.</p></div><a href="/industry-niche-atlas-plan.md" className="rounded-full bg-[#c75b4a] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#a94c3e]">Open complete plan <ArrowRight className="ml-2 inline h-4 w-4" /></a></div></section></main><footer className="bg-[#242822] text-[#b8b9ae]"><div className="container flex flex-col justify-between gap-4 py-8 text-xs sm:flex-row sm:items-center"><Logo small /><div className="flex flex-wrap gap-x-5 gap-y-2"><span>Research prototype</span><span>Data sources named</span><span>Static front-end</span></div></div></footer></div>;
}
